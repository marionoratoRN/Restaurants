import {DEFAULT_COLOURS} from '../colors';
import {palette} from '../palette';

export const colors = {
  [DEFAULT_COLOURS.WHITE]: palette.white,
  [DEFAULT_COLOURS.BASE_TEXT]: palette.grey,
  [DEFAULT_COLOURS.NANDO]: palette.red,
};
